# Case Study: Customer Complaint Tokenization System

## 1. Case Study Title

**Tokenizing Customer Complaints for Automated Support Analysis**

## 2. Business Background

An e-commerce company receives thousands of customer messages every day through:

* Email
* Chat support
* Mobile application
* Website complaint forms
* Social media

The messages are written in natural language and may contain complaints related to delivery, payment, product quality, refund, replacement, or customer service.

The company wants to process these messages automatically. Before applying text classification or sentiment analysis, the messages must first be broken into smaller units called **tokens**.

## 3. Business Problem

Customer-support agents currently read every complaint manually.

This creates several problems:

* Complaints are not processed quickly.
* Important words may be missed.
* Similar complaints are handled inconsistently.
* Urgent complaints may not be prioritized.
* Manual analysis becomes difficult when message volume increases.

The company wants to build a tokenization component that converts customer messages into individual words and sentences.

## 4. Objective

The objective is to create a text-processing system that can:

* Read customer complaint messages.
* Divide paragraphs into sentences.
* Divide sentences into words.
* Handle punctuation correctly.
* Convert text into a consistent format.
* Preserve important business terms.
* Prepare tokens for later NLP processing.

## 5. Sample Customer Complaints

| Complaint ID | Customer Message                                                |
| ------------ | --------------------------------------------------------------- |
| C001         | My order arrived three days late and the package was damaged.   |
| C002         | I paid ₹2,500, but the payment status still shows pending.      |
| C003         | The product is not working. Please replace it immediately.      |
| C004         | I requested a refund last week, but I have not received it yet. |
| C005         | The delivery agent was rude and refused to deliver the package. |
| C006         | I ordered a blue shirt, but I received a black shirt.           |
| C007         | The mobile phone battery drains within two hours.               |
| C008         | My order number ORD-45891 has not been shipped.                 |
| C009         | The app shows “Payment Failed”, but the amount was deducted.    |
| C010         | Please cancel my order and return the payment to my account.    |

## 6. Expected Tokenization Types

The system must support the following tokenization methods.

### Sentence Tokenization

Sentence tokenization divides a paragraph into individual sentences.

Example input:

```text
The product is damaged. Please replace it immediately.
```

Expected structure:

```text
Sentence 1: The product is damaged.
Sentence 2: Please replace it immediately.
```

### Word Tokenization

Word tokenization divides a sentence into individual words or symbols.

Example input:

```text
My order arrived late.
```

Expected structure:

```text
My
order
arrived
late
.
```

### Character Tokenization

Character tokenization divides text into individual characters.

Example input:

```text
late
```

Expected structure:

```text
l
a
t
e
```

### Subword Tokenization

Subword tokenization divides complex or unknown words into smaller meaningful parts.

Example:

```text
unacceptable
```

Possible subword structure:

```text
un
accept
able
```

## 7. Functional Requirements

The tokenization system should:

1. Accept a customer complaint as input.
2. Remove unnecessary spaces.
3. Convert text to lowercase when required.
4. Separate sentences using punctuation marks.
5. Separate words using spaces and punctuation.
6. Retain important numbers such as order IDs and payment amounts.
7. Handle contractions such as:

   * cannot
   * didn’t
   * hasn’t
8. Handle special characters such as:

   * ₹
   * %
   * *
   * /
9. Handle abbreviations such as:

   * Mr.
   * Dr.
   * ETA
10. Handle product codes and order numbers such as:

* ORD-45891
* PROD-1005

11. Preserve meaningful business phrases where necessary.
12. Generate tokens in a structured format.

## 8. Important Tokenization Challenges

### Challenge 1: Punctuation

Input:

```text
The product is damaged!
```

The system must decide whether the exclamation mark should be treated as a separate token.

### Challenge 2: Currency Values

Input:

```text
I paid ₹2,500 for the product.
```

The system must preserve the currency value correctly.

### Challenge 3: Order Numbers

Input:

```text
My order ORD-45891 is delayed.
```

The system should avoid unnecessarily separating the order number.

### Challenge 4: Contractions

Input:

```text
I haven't received my refund.
```

The system must decide whether to treat:

```text
haven't
```

as one token or divide it into:

```text
have
n't
```

### Challenge 5: Multiple Sentences

Input:

```text
The package arrived late. The product was also damaged. I need a replacement.
```

The system must identify all three sentences correctly.

### Challenge 6: Mixed Language

Input:

```text
My order abhi tak deliver nahi hua.
```

The system must handle English and transliterated Hindi words.

### Challenge 7: Emojis

Input:

```text
Very disappointed with the service 😡
```

The system must decide whether the emoji should be preserved as a token.

### Challenge 8: URLs and Email Addresses

Input:

```text
I sent the details to support@example.com.
```

The email address should not be broken incorrectly.

## 9. Task List

### Task 1: Understand the Complaint Data

Review the sample customer complaints and identify:

* Sentences
* Words
* Numbers
* Punctuation
* Order IDs
* Currency values
* Emojis
* Special characters

### Task 2: Perform Sentence Tokenization

Divide each customer complaint into separate sentences.

### Task 3: Perform Word Tokenization

Divide every sentence into individual tokens.

### Task 4: Normalize the Text

Decide whether the following operations should be applied:

* Lowercase conversion
* Extra-space removal
* Punctuation removal
* Special-character handling

### Task 5: Preserve Business Information

Ensure that important values are retained correctly, including:

* Order numbers
* Payment amounts
* Product codes
* Dates
* Email addresses
* Phone numbers

### Task 6: Compare Tokenization Methods

Compare the results of:

* Space-based tokenization
* Rule-based tokenization
* Library-based tokenization
* Subword tokenization

### Task 7: Handle Special Cases

Test the tokenizer with:

* Contractions
* Abbreviations
* Currency values
* Emojis
* URLs
* Email addresses
* Hyphenated words
* Mixed-language messages

### Task 8: Prepare Structured Output

Create an output structure containing:

| Field           | Description                                    |
| --------------- | ---------------------------------------------- |
| Complaint ID    | Unique complaint number                        |
| Original Text   | Complete customer complaint                    |
| Sentence Count  | Number of sentences                            |
| Word Count      | Number of word tokens                          |
| Sentence Tokens | List of extracted sentences                    |
| Word Tokens     | List of extracted words                        |
| Special Tokens  | Order IDs, numbers, currency, emojis, or codes |

## 10. Additional Test Messages

```text
My payment of ₹4,999 was deducted, but the order was not confirmed.
```

```text
Order ID ORD-98765 was expected on 20 June, but it has not arrived.
```

```text
I can't log in to my account. Please help!
```

```text
The product quality is unacceptable 😡 I need a refund.
```

```text
Contact me at customer123@gmail.com regarding my complaint.
```

```text
The customer-care executive said the replacement would take 5-7 days.
```

```text
Mera order abhi tak deliver nahi hua.
```

```text
The app displayed error-code PAY-504 during payment.
```

## 11. Expected Business Outcome

After successful tokenization, the output can be used in later NLP processes such as:

* Complaint classification
* Sentiment analysis
* Keyword extraction
* Named Entity Recognition
* Automatic ticket routing
* Priority identification
* Chatbot response generation

## 12. Expected Deliverables

The learner should prepare:

* Tokenized complaint data
* Sentence-level token output
* Word-level token output
* Special-token identification
* Comparison of tokenization approaches
* Observations about tokenization challenges
* Final structured output file
* Brief business interpretation

## 13. Evaluation Criteria

| Evaluation Area            | Description                                        |
| -------------------------- | -------------------------------------------------- |
| Sentence accuracy          | Sentences are separated correctly                  |
| Word accuracy              | Words are tokenized correctly                      |
| Punctuation handling       | Punctuation is handled consistently                |
| Business data preservation | Order IDs, amounts, and codes remain meaningful    |
| Special cases              | Contractions, emojis, URLs, and emails are handled |
| Output structure           | Results are presented clearly                      |
| Business relevance         | Tokens are suitable for further NLP processing     |

## 14. Final Problem Statement

Design a tokenization system for customer complaint messages that divides unstructured text into meaningful sentence and word tokens while preserving important business information such as order IDs, payment amounts, product codes, email addresses, and emojis.

The output must be suitable for later use in complaint classification, sentiment analysis, and automated customer-support routing.

import re
import pandas as pd


# ------------------------------------------------------------
# 1. CREATE DATASET
# ------------------------------------------------------------

data = {
    "complaint_id": [
        "C001",
        "C002",
        "C003",
        "C004",
        "C005",
        "C006",
        "C007",
        "C008"
    ],

    "customer_message": [
        "My order arrived three days late and the package was damaged.",
        "I paid ₹2,500, but the payment status still shows pending.",
        "The product is not working. Please replace it immediately.",
        "I requested a refund last week, but I have not received it yet.",
        "My order number ORD-45891 has not been shipped.",
        "The product quality is unacceptable 😡 I need a refund.",
        "Contact me at customer123@gmail.com regarding my complaint.",
        "I can't log in to my account. Please help!"
    ]
}

df = pd.DataFrame(data)


# ------------------------------------------------------------
# 2. NORMALIZE TEXT
# ------------------------------------------------------------

def normalize_text(text):
    text = text.replace("’", "'")
    text = text.replace("“", '"')
    text = text.replace("”", '"')
    text = re.sub(r"\s+", " ", text)

    return text.strip()


df["normalized_text"] = df["customer_message"].apply(
    normalize_text
)


# ------------------------------------------------------------
# 3. SENTENCE TOKENIZATION
# ------------------------------------------------------------

def sentence_tokenize(text):
    sentences = re.split(
        r"(?<=[.!?])\s+",
        text
    )

    return [
        sentence.strip()
        for sentence in sentences
        if sentence.strip()
    ]


df["sentence_tokens"] = df["normalized_text"].apply(
    sentence_tokenize
)


# ------------------------------------------------------------
# 4. WORD TOKENIZATION
# ------------------------------------------------------------

TOKEN_PATTERN = re.compile(
    r"""
    [\w.+-]+@[\w.-]+\.[A-Za-z]{2,}
    |
    ₹\d+(?:,\d{3})*(?:\.\d+)?
    |
    [A-Z]{2,}-\d+
    |
    [A-Za-z]+(?:'[A-Za-z]+)?
    |
    \d+(?:\.\d+)?
    |
    [\U0001F300-\U0001FAFF]
    |
    [^\w\s]
    """,
    re.VERBOSE
)


def word_tokenize(text):
    return TOKEN_PATTERN.findall(text)


df["word_tokens"] = df["normalized_text"].apply(
    word_tokenize
)


# ------------------------------------------------------------
# 5. SENTENCE COUNT
# ------------------------------------------------------------

df["sentence_count"] = df["sentence_tokens"].apply(len)


# ------------------------------------------------------------
# 6. WORD COUNT
# ------------------------------------------------------------

def count_meaningful_tokens(tokens):
    count = 0

    for token in tokens:

        if re.search(r"[A-Za-z0-9]", token):
            count += 1

        elif token.startswith("₹"):
            count += 1

        elif re.fullmatch(
            r"[\U0001F300-\U0001FAFF]",
            token
        ):
            count += 1

    return count


df["word_count"] = df["word_tokens"].apply(
    count_meaningful_tokens
)


# ------------------------------------------------------------
# 7. SPECIAL TOKEN EXTRACTION
# ------------------------------------------------------------

def extract_special_tokens(tokens):
    special_tokens = {
        "order_ids": [],
        "amounts": [],
        "emails": [],
        "emojis": []
    }

    for token in tokens:

        if re.fullmatch(r"[A-Z]{2,}-\d+", token):
            special_tokens["order_ids"].append(token)

        elif re.fullmatch(
            r"₹\d+(?:,\d{3})*(?:\.\d+)?",
            token
        ):
            special_tokens["amounts"].append(token)

        elif re.fullmatch(
            r"[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}",
            token
        ):
            special_tokens["emails"].append(token)

        elif re.fullmatch(
            r"[\U0001F300-\U0001FAFF]",
            token
        ):
            special_tokens["emojis"].append(token)

    return {
        key: value
        for key, value in special_tokens.items()
        if value
    }


df["special_tokens"] = df["word_tokens"].apply(
    extract_special_tokens
)


# ------------------------------------------------------------
# 8. FINAL RESULT
# ------------------------------------------------------------

result = df[
    [
        "complaint_id",
        "customer_message",
        "sentence_count",
        "word_count",
        "sentence_tokens",
        "word_tokens",
        "special_tokens"
    ]
]

pd.set_option("display.max_colwidth", None)

print(result.to_string(index=False))


# ------------------------------------------------------------
# 9. SAVE OUTPUT
# ------------------------------------------------------------

result.to_csv(
    "tokenized_customer_complaints.csv",
    index=False,
    encoding="utf-8-sig"
)

print(
    "\nOutput saved as tokenized_customer_complaints.csv"
)
